{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Moji Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--new-window",
        "--disable-extensions",
        "--user-data-dir=${workspaceFolder}/.vscode-test/user-data",
        "--extensions-dir=${workspaceFolder}/.vscode-test/extensions",
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}"
      ]
    }
  ]
}
